# --- Database ---

# Database connection URL.
DATABASE_URL=

# --- Application ---

# Runtime mode.
NODE_ENV=development

# Fixture-only var, statically read AND declared in Zod — pins liveness so
# this case cannot pass merely because the scanner failed to walk src/**.
SRC_READ_DYNAMIC_KEY_DECLARED=
