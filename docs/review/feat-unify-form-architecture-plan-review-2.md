# プランレビュー: feat/unify-form-architecture
日時: 2026-03-02T00:00:00+09:00
レビュー回数: 2回目

## 前回からの変更
前回の15件採用を反映済み。今回は更新されたプランを再評価。

## 機能観点の指摘

### F-1: team-archived-list.tsx の editData がLoginフィールドしか復元しない (採用)
- **問題**: team-archived-list.tsx の editData は Login 用フィールドしか持たず、非Login エントリ編集でフィールド消失リスク
- **推奨対応**: Phase 4 で editData 型を全フィールド網羅に拡張

### F-2: ページルート削除のブックマーク影響 (却下)
- **問題**: 直接URLアクセスが404になる
- **判定**: パスワードマネージャーでは特定エントリURLのブックマークは実質不使用。UIからの導線がなく、影響は極小

### F-3: Phase 2 スコープ明確化 (却下)
- **判定**: プランに既に十分な詳細あり

### F-4: Phase 3a/3c の Dialog 作成責務重複 (採用)
- **推奨対応**: Phase 3a のリストからDialog関連を除外し、3c に統一

### F-5: Phase 3d 削除候補が15+ファイル (採用)
- **問題**: 実際の削除候補は3ファイルではなく15+ファイル（テスト含め30+）
- **推奨対応**: 削除候補をカテゴリ分けして列挙

### F-6: Phase 3b ベースフック責務 (却下)
- **判定**: プランに既に記載済み。実装時の詳細

## セキュリティ観点の指摘

### S-6: overviewBlobにentryType欠落 (採用 — 注意事項)
- **推奨対応**: 型定義設計時にentryType包含可否を決定し文書化

### S-7: MEMBER UI権限分岐の不備 (採用)
- **問題**: MEMBERが他人のエントリの編集ボタンを見れる（サーバーは403返却）
- **推奨対応**: Phase 4で createdBy.id === session.user.id 分岐追加

### S-8: チーム用Zodスキーマのid必須refinement欠落 (採用)
- **問題**: createTeamE2EPasswordSchemaに個人用と同様のrefinement欠落
- **推奨対応**: refinement追加（Phase 3と並行で実施可能）

### S-9: submitヘルパーAADバイパスリスク (採用 — 制約強化)
- **推奨対応**: セキュリティ制約に「saveTeamEntryが唯一のAPIフェッチパス」を明記

## テスト観点の指摘

### T-7: vi.mock更新対象4ファイルでは不足 (採用)
- **推奨対応**: grepベース網羅チェックを義務化

### T-8: 移植テストケース名の特定精度 (採用)
- **推奨対応**: 「全テストケース移植」に変更

### T-9: ベースフック具体的モジュール名 (却下)
- **判定**: 実装時の詳細であり、プランレベルでは不要

### T-10: grep分離実行 (採用)
- **推奨対応**: プロダクションコードとテストコードで分離grepに更新

### T-11: coverage.include にhooks未含有 (却下)
- **判定**: プランのスコープ外（インフラ設定）

### T-12: 手動テストマトリクス内容確認 (却下)
- **判定**: プランに既に記載あり
