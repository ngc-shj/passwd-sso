# プランレビュー: import/export リネーム

日時: 2026-02-22
レビュー回数: 1回目

## 前回からの変更

初回レビュー

## 機能観点の指摘

| ID | 深刻度 | 概要 | 対応 |
|----|--------|------|------|
| F1 | 高 | 置換順序: `export-dialog` → `password-export` が `org-export-dialog` → `org-export` より先に記載。一括置換で `org-export-dialog` が `org-password-export` に誤変換される | 採用: `org-export-dialog` を先に置換するよう順序修正 |

## セキュリティ観点の指摘

指摘なし。純粋なファイルリネームであり、ロジック・暗号・認証・認可への変更なし。

## テスト観点の指摘

| ID | 深刻度 | 概要 | 対応 |
|----|--------|------|------|
| T1 | 致命的 | 置換順序が危険 (`import-dialog` を先に置換すると誤変換) | F1 と同一。プランの記載順序は正しい（長い文字列が先）が、`org-export-dialog` の順序を修正 |
| T2 | 高 | 相対パスインポート (`./import-dialog-parsers`) の見落とし | 不採用: ステップ2の文字列置換はパス形式に関係なく部分一致で置換。問題なし |
| T3 | 中 | コメント内に旧ファイル名が残存する可能性 | 採用: リネーム後に旧名の残存を grep で確認するステップを追加 |
| T4 | 中 | hook ファイルと vi.mock パスの同時更新が必要 | 不採用: ステップ2で全パスを同時一括置換する手順が記載済み |
