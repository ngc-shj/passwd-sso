# 手動テスト項目: feat-reprompt

対象ブランチ: `feat/reprompt`
作成日: 2026-02-18

## 1. エントリ作成・編集

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 1-1 | 新規作成画面で「マスターパスワード再確認を要求」チェックボックスが表示される | チェックボックスが表示される |
| 1-2 | チェックを入れて保存 → ダイアログ編集を開く | チェックが入った状態で表示される |
| 1-3 | チェックを入れて保存 → `/dashboard/[id]/edit` ページを開く | チェックが入った状態で表示される |
| 1-4 | チェックを外して保存 → 再度編集を開く | チェックが外れた状態で表示される |
| 1-5 | チェックなしで新規作成 → 編集を開く | チェックが外れた状態で表示される |

## 2. 一覧表示

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 2-1 | reprompt ON のエントリにシールドアイコン (ShieldCheck) が表示される | タイトル横にアイコン表示 |
| 2-2 | reprompt OFF のエントリにはシールドアイコンが表示されない | アイコンなし |
| 2-3 | ON/OFF 混在時に正しいエントリにのみアイコンが表示される | 対象エントリのみ |

## 3. 詳細画面 — パスワード表示・コピー

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 3-1 | reprompt ON: パスワード表示 (Eye ボタン) をクリック | 再確認ダイアログが表示される |
| 3-2 | 再確認ダイアログで正しいパスフレーズを入力 → 確認 | パスワードが表示される (30秒後自動非表示) |
| 3-3 | 再確認ダイアログで誤ったパスフレーズを入力 → 確認 | エラーメッセージ表示、ダイアログは閉じない |
| 3-4 | 再確認ダイアログでキャンセル | ダイアログ閉じ、パスワードは非表示のまま |
| 3-5 | reprompt ON: パスワードコピー (CopyButton) をクリック | 再確認ダイアログ表示 → 検証後コピーされる |
| 3-6 | reprompt OFF: パスワード表示 (Eye ボタン) をクリック | 再確認なしでパスワード表示 |
| 3-7 | reprompt OFF: パスワードコピーをクリック | 再確認なしでコピーされる |

## 4. 詳細画面 — TOTP コピー

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 4-1 | reprompt ON + TOTP 設定済み: TOTP コピーをクリック | 再確認ダイアログ表示 → 検証後コピー |
| 4-2 | reprompt OFF + TOTP 設定済み: TOTP コピーをクリック | 再確認なしでコピー |

## 5. 詳細画面 — HIDDEN カスタムフィールド

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 5-1 | reprompt ON + HIDDEN フィールド: 表示 (Eye) をクリック | 再確認ダイアログ → 検証後表示 |
| 5-2 | reprompt ON + HIDDEN フィールド: コピーをクリック | 再確認ダイアログ → 検証後コピー |
| 5-3 | reprompt OFF + HIDDEN フィールド: 表示をクリック | 再確認なしで表示 |
| 5-4 | TEXT / URL カスタムフィールドは reprompt ON でも再確認不要 | 直接表示・コピーされる |

## 6. 詳細画面 — パスワード履歴

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 6-1 | reprompt ON: 履歴の表示 (Eye) をクリック | 再確認ダイアログ → 検証後表示 |
| 6-2 | reprompt ON: 履歴のコピーをクリック | 再確認ダイアログ → 検証後コピー |
| 6-3 | reprompt OFF: 履歴の表示・コピー | 再確認なしで操作可能 |

## 7. 検証キャッシュ (30秒 TTL)

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 7-1 | 検証成功直後に同エントリで別操作 (例: 表示→コピー) | 再確認不要 (キャッシュヒット) |
| 7-2 | 検証成功から 30秒以内に操作 | 再確認不要 |
| 7-3 | 検証成功から 30秒以上経過後に操作 | 再確認ダイアログが再表示される |
| 7-4 | エントリ A で検証成功後、エントリ B (reprompt ON) で操作 | エントリ B は再確認必要 (スコープはエントリ単位) |

## 8. 再確認ダイアログ UX

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 8-1 | Enter キーで検証送信 | パスフレーズ入力後 Enter で検証が実行される |
| 8-2 | 検証中 (ローディング中) にキャンセルボタン | disabled で押せない |
| 8-3 | 検証中に ESC キー / オーバーレイクリック | ダイアログは閉じない |
| 8-4 | 空のパスフレーズで確認ボタン | disabled で押せない |

## 9. Import / Export

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 9-1 | reprompt ON のエントリを CSV エクスポート | `reprompt` 列が `1` |
| 9-2 | reprompt OFF のエントリを CSV エクスポート | `reprompt` 列が空文字 |
| 9-3 | reprompt ON のエントリを JSON エクスポート | `requireReprompt: true` が含まれる |
| 9-4 | CSV `reprompt=1` をインポート | エントリの reprompt が ON になる |
| 9-5 | CSV `reprompt=""` をインポート | エントリの reprompt が OFF になる |
| 9-6 | CSV `reprompt="true"` をインポート | reprompt OFF (厳密一致: `"1"` のみ true) |
| 9-7 | JSON `requireReprompt: true` をインポート | エントリの reprompt が ON |
| 9-8 | JSON `requireReprompt: 1` (非boolean) をインポート | reprompt OFF (strict boolean) |

## 10. パスフレーズ変更後

| # | テスト内容 | 期待結果 |
|---|----------|---------|
| 10-1 | パスフレーズ変更後、reprompt ON エントリで操作 | 新しいパスフレーズで再確認ダイアログが動作する |
| 10-2 | パスフレーズ変更後、旧パスフレーズを入力 | エラー表示 (旧パスフレーズは拒否) |
